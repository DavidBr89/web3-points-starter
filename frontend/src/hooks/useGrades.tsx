// GRADE: 1

export const useGrades = () => {
  // TODO: Gebruik de useContext hook om de GradesContext te gebruiken en de context terug te geven.
  if (!context) {
    throw new Error("useGrades must be used within a GradesProvider");
  }
  // TODO: Stuur de context terug, zodat we deze kunnen gebruiken in de componenten die deze hook aanroepen.
};
